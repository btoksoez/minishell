#include "../includes/minishell.h"

/* if no args: show all env, (but sorted?)
for each arg: 
	check if equal sign:
		no: add to envp with =''
		yes: add with value after= */
int	mini_export(t_shell *shell, t_tree_node *tree)
{
	//TODO
	(void)shell;
	(void)tree;
	return (1);
}
